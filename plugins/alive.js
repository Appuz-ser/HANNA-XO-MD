const { runtime, isPrivate } = require("../lib");

command(
	{
		pattern: "he",
		desc: "Alive Bot",
		fromMe: true,
		type: "user"
},
async (conn, match) => {
const sender = m.pushName
	await conn.reply(`𝐈’𝐦 𝐀𝐥𝐢𝐯𝐞 𝐁𝐫𝐨 ${sender}`)
});
